# 产品配置管理系统 AI 工作约定

## 默认模型路由

处理本项目的知识库、注册资料、产品材料、功能身份、问答、系统开发或质量审计任务时，默认自动应用 `.agents/skills/vinno-model-routing/SKILL.md`，不要求用户重复指定技能或模型。

- 根据当前步骤在 `gpt-5.6-luna`、`gpt-5.6-terra` 和 `gpt-5.6-sol` 中选择最低充分模型。
- 环境支持内部任务或分阶段模型选择时，自动完成分派和交接，不要求用户输入路由口令。
- 用户明确指定模型、推理强度或禁止切换时，以用户当次要求为准。
- 模型选择不扩大操作权限；正式发布、注册证启停、批量覆盖正式数据及其他高影响写操作仍需用户确认。
- 环境不支持模型切换时继续使用当前模型，不虚构已切换；仅当限制会实质影响结果时提示用户。

具体业务规则继续由 `.agents/skills/` 下对应的VINNO业务技能提供，模型路由技能不替代业务技能。
